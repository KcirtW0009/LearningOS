# Resolver capability contracts.
#
# Consumer-owned contracts that define what the Resolver requires
# from its architectural providers (State Authority, Graph layer).
#
# Placement rule (CPR-003):
#   Contracts live at the consumer's architectural boundary.
#   Resolver defines what it needs — providers satisfy structurally.
